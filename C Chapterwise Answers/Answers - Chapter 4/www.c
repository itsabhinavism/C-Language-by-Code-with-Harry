#include <stdio.h>

float main() {
    float a,sum = a;
    float i;
    printf("Type the value - ");
    scanf("%f",&a);
    do {
        sum += i;
        i++;
    } while (i <= 10);
    printf("The sum of the first ten numbers is %d\n", sum);
    return 0;
}
